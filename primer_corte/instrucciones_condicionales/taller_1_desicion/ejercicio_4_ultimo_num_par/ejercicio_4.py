# Ultimo número es par?

# Input
n = int(input("Digite un número entero(z): "))

# Prosessing
if n % 2 == 0:
    z = "El ultimo digito es par"
else:
    z = "El ultimo digito no es par"

# Output
print(z)